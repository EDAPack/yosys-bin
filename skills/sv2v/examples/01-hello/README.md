# 01-hello

Intent: take a small SystemVerilog file (packed struct, `always_ff`,
`import package::*`), translate it to Verilog-2005 with sv2v, and
confirm yosys's plain Verilog frontend accepts the result.

```
./run.sh
```
